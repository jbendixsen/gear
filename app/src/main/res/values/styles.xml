<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="GearBody" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/body</item>
    </style>

    <style name="GearDisplay" parent="@android:style/TextAppearance.Large">
        <item name="android:textColor">@color/display_text</item>
        <item name="android:textSize">45sp</item>
    </style>

    <style name="GearRatio" parent="GearDisplay">
        <item name="android:textSize">24sp</item>
    </style>

    <style name="Gear" parent="GearBody">
        <item name="android:textSize">12sp</item>
    </style>

    <style name="Speed" parent="GearBody">
        <item name="android:textSize">20sp</item>
    </style>

</resources>
